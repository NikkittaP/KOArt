# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

tests/_output/*
tests/_support/_generated

#vagrant folder
/.vagrant

# === Secrets — never commit. Copy .env.example to .env and fill in locally. ===
# (config/db.php and config/web.php now read secrets from .env, so they are
#  safe to commit. Only .env itself must stay out of git.)
/.env

# === User content & generated images (live only on the server / locally) ===
/web/paintings_photo/
/web/series_cover/
/web/about_photo/
/web/uploads/

# === Yii runtime & published assets ===
/web/assets/
/runtime/logs/*
/runtime/cache/*
/runtime/debug/*

# === OSPanel per-project config (local only) ===
/.osp/

# === Editor ===
/.vscode