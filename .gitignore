# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

tests/_output/*
tests/_support/_generated

#vagrant folder
/.vagrant

# === Secrets — never commit. Copy the *.example file and fill in locally. ===
/.env
/config/db.php
/config/web.php
/models/User.php

# === User content & generated images (live only on the server / locally) ===
/web/paintings_photo/
/web/series_cover/
/web/about_photo/
/web/uploads/

# === Yii runtime & published assets ===
/web/assets/
/runtime/logs/*
/runtime/cache/*
/runtime/debug/*

# === OSPanel per-project config (local only) ===
/.osp/

# === Editor ===
/.vscode